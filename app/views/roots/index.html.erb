<div class="container roots">
    <%= render :partial=> 'form' %>
    <h2><%= pluralize(@current_user.roots.count, 'concept family') %></h2>
    <ul class="tree">
        <% @current_user.roots.each do |root| %>
        <%# raise 'hell' %>
        <li>
            <details>
                <summary>
                    <%= link_to root.text.upcase() , root_path(root.id) %>
                    <span>(<%= pluralize(root.concepts.count, "concept") %>)</span>
                </summary>
                <ul>
                    <% root.concepts.each do |concept| %>
                    <li>
                        <details>
                            <summary>
                                <%= link_to concept.text.capitalize(), concept_path(concept.id) %>
                            </summary>
                            <ul>
                                <% concept.linking_concepts.each do |lc| %>
                                <li>
                                    <%= link_to lc.text.capitalize(), concept_path(lc.id) %>
                                </li>
                                <% end %>
                            </ul>
                        </details>
                    </li>
                    <% end %>
                </ul>

            </details>
        </li>
        <% end %>
    </ul>
</div>

